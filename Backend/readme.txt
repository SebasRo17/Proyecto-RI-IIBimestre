# Instalaciones Necesarias!!!
# Instalarlo en el directorio raiz carpeta PROYECTO RI IIBIMESTRE

pip install transformers datasets torchvision faiss-cpu

pip install fastapi uvicorn[standard] pillow transformers faiss-cpu
